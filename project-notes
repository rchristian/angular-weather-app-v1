General:

	remove top nav bar
	remove corresponding scripts from index
	
	check bottom nav - is it tabbed? if not use tabs?

	Local (user location) Weather
	F <-> C (in header?)
	5 columns for five day



Javascript:

	add/remove hidden on alert if alert

	click F/C to change

	how-to ng-repeat five day

	fahren <--> change URL?

	change url based on 'active' class?
	?	
		if(F active class) {
			tempURL = &units=imperial
		} else if (C active class) {
			tempURL = &units=metric
		}
	?



	use yahoo!weather? no API needed



	apiid = f72650c40fef189a346723016e3b5ca6;

	JSON call for useR location
	http://ip.pycox.com/json


	url + ?q= + location + &appid= + apiid + tempUnits + callback
	For temperature in Fahrenheit use &units=imperial
	For temperature in Celsius use &units=metric
	JSONP callback = &callback=test


	//switch statement for icons?


	CURRENT
	URL = api.openweathermap.org/data/2.5/weather?q=London,uk&appid=f72650c40fef189a346723016e3b5ca6

	5 DAY
	URL = api.openweathermap.org/data/2.5/forecast;
